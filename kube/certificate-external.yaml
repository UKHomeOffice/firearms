apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: branch-tls-external
spec:
  commonName: "*.firearms-branch.homeoffice.gov.uk"
  dnsNames:
  - "*.firearms-branch.homeoffice.gov.uk"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: branch-tls-external
